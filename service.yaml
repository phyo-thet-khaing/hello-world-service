apiVersion: v1
kind: Service
metadata:
  name: hello-world-service
  namespace: staging
spec:
  type: NodePort
  selector:
    app: hello-world # Must match deployment's pod labels
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
